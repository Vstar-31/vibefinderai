# Core Web Framework
fastapi>=0.103.0

# ASGI Server
uvicorn[standard]>=0.23.2

# JWT Authentication
PyJWT>=2.8.0

# Database ORM
prisma>=0.11.0

# Password Hashing & Security
bcrypt==3.2.2
passlib[bcrypt]>=1.7.4

# Form Data Parsing (Required for OAuth2 Login)
python-multipart>=0.0.9

# HTTP clients
aiohttp
requests

# Rate limiting
slowapi

# ML — disabled on free tier (512MB RAM limit)
# fastembed + onnxruntime cost ~300MB installed + ~150MB loaded.
# Re-enable by adding fastembed>=0.2.6 here AND setting ENABLE_SEMANTIC=true in Render env vars.
# fastembed>=0.2.6   # uncomment for paid Render instances only
scikit-learn

# Optional: sentiment boost (uncomment to enable)
textblob
python-dotenv

httpx>=0.27.0
python-jose[cryptography]