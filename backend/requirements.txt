# Core Web Framework
fastapi>=0.103.0

# ASGI Server
uvicorn[standard]>=0.23.2

# JWT Authentication
PyJWT>=2.8.0

# Database ORM
prisma>=0.11.0

# Password Hashing & Security
bcrypt==3.2.2
passlib[bcrypt]>=1.7.4

# Form Data Parsing (Required for OAuth2 Login)
python-multipart>=0.0.9

# HTTP clients
aiohttp
requests

# Rate limiting
slowapi

# ML — lightweight only
# fastembed replaces sentence-transformers to stay under Render's 512MB RAM free tier limit.
fastembed>=0.2.6
scikit-learn

# Optional: sentiment boost (uncomment to enable)
# textblob
python-dotenv

httpx>=0.27.0
python-jose[cryptography]